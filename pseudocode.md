#Overview
we need a button that will go grab a photo from NASA
we need elements on the page that can hold the photo, title, and description
we need an input box where we can specify a date
    - that date will be used to get the picture of the day from NASA
we need maybe one more element that will specify whether or not the picture contains Jupiter